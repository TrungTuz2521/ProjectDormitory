@{
    ViewData["Title"] = "Không có quyền";
    Layout = null;
}
<h3>Bạn không có quyền truy cập trang này.</h3>
@if (ViewBag.ReturnUrl != null)
{
    <p><a href="@ViewBag.ReturnUrl">Quay lại trang trước</a></p>
}
